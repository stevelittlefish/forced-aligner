# Dev/test tooling only — deliberately separate from requirements.txt so the
# CUDA container stays lean (no test stack shipped to the AI server).
#
# Note this does NOT pull in whisperx/torch: the tests mock the ML stack, so
# they run on a laptop with no GPU. Only the light HTTP + tooling deps are here.
# (For a full end-to-end run against real audio you still need requirements.txt.)
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
numpy>=1.24
pytest>=8.0
httpx>=0.27        # FastAPI's TestClient transport
ruff>=0.5
